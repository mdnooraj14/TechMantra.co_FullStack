spring.application.name=TechMantraRegistration
server.port=8080

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/registration_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# If you want to disable security, you may need to configure it in your security config.
# spring.security.enabled=false  <-- verify if required

# ==========================
# \u2705 SMTP Configuration (Add This)
# ==========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username= Your Email
spring.mail.password=Use Google App Passwords
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
